#include <stdio.h> 
#include <stdlib.h>

int main(int argc, char *argv[])
{
	int i = 1;
	int number1 = atoi(argv[1]);
	int trial = i;

	while (number1 >= i) {
	i++;
} 
if(number1 > i){

printf("%d\n", trial);
	trial;
}
	return 0;
}
