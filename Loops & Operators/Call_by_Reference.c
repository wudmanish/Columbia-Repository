#include <stdio.h>
int main()
{
	printf("Input the first number : %d\n"
	scanf("%d", n);
	printf("input the second number : %d\n"
	scanf("%d", o);
	
	if(n > o){
	printf("%d is greater than %d\n", n, o);
}

	if(o > n){
	printf("%d is greater than %d\n", o, n);
}
