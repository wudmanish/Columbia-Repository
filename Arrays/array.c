#include <stdio.h>
int main(int argc, char *argv[])
{
	int cool[10];
	int i, j, sum=0;

	for (i = 0; i < 10; i++){
cool[i] = i + 0;
}
	for (j=0; j < 10; j++){
	printf("Element[%d] = %d\n", j, cool[j]);
}
 sum = cool[10];
	printf("The sum is: %d\n.", sum);
}
